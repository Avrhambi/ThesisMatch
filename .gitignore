node_modules
.next
out
dist
build
.env
.env.local
npm-debug.log*
*.tsbuildinfo
next-env.d.ts

# symlink to .agents/skills/hallmark (git on Windows resolves it to real
# files instead of a symlink entry, duplicating the whole skill)
.claude/skills

# Playwright MCP session output (screenshots, snapshots, console/network logs)
.playwright-mcp/
